platform :ios, '9.0'
use_frameworks!
inhibit_all_warnings!

target 'GithubClient' do
	pod 'Alamofire' 
	pod 'SwiftyJSON'
    
    target 'GithubClientTests' do
        inherit! :search_paths
        pod 'Alamofire'
    end
    
end
